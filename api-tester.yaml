apiVersion: v1
kind: Pod
metadata:
  name: api-tester
  namespace: lab
spec:
  serviceAccountName: app2-sa
  containers:
  - name: curl
    image: curlimages/curl:8.7.1
    command: ["sh", "-c", "sleep 3600"]
  restartPolicy: Never
